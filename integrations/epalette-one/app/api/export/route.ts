import { identity,readState,responseError } from '@/lib/store';
export async function GET(){try{const who=await identity();const s=await readState(who.owner);const cell=(v:unknown)=>{let t=String(v??'');if(/^[=+@-]/.test(t))t="'"+t;return '"'+t.replaceAll('"','""')+'"'};const rows=[['データ種別','車両','予定','開始','終了','状態','走行計画km'],...s.bookings.map(b=>['模擬データ',b.vehicleId,b.title,b.start,b.end,b.status,b.distance])];return new Response('\uFEFF'+rows.map(r=>r.map(cell).join(',')).join('\r\n'),{headers:{'Content-Type':'text/csv;charset=utf-8','Content-Disposition':'attachment; filename="epalette-one-demo.csv"','Cache-Control':'no-store'}});}catch(e){return responseError(e);}}
